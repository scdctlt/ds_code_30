#include<bits/stdc++.h>
using namespace std;
typedef struct LNode
{
    /* data */
    int data;
    struct LNode *next;
}LNOde,*LinkList;


LinkList rever_LinkList(LinkList &L,int n,int l,int r){
    if(L==NULL) return L;
    int num;
    LNode *p=L->next,*s,*t,*q,*e;

    for (int i = 0; p; i++)
    {
        if (i=l-1) e=p;        
        if (i>=l)
        {
              
        }
        
    }
    
     
    
    


}